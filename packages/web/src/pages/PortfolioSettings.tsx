// packages/web/src/pages/PortfolioSettings.tsx — stub placeholder (Task 5b.4 fleshes this out)
import { useDocumentTitle } from '@/hooks/useDocumentTitle';

export default function PortfolioSettings() {
  useDocumentTitle('Settings');
  return <div>portfolio settings</div>;
}
