// packages/web/src/pages/UserSettings.tsx — stub placeholder (Task 5b.5 fleshes this out)
import { useEffect } from 'react';

export default function UserSettings() {
  useEffect(() => { document.title = 'User settings · quovibe'; }, []);
  return <div>user settings</div>;
}
